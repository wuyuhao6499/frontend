version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install
  build:
    commands:
      - NODE_OPTIONS=--openssl-legacy-provider ng build --project=shop
artifacts:
  files:
    - '**/*'
  base-directory: dist/shop
